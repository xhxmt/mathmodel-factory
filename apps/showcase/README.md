# Showcase boundary

The public paper showcase is served by `web/backend/showcase_api.py`; static
demonstration assets remain in `demo_site/` until the compatibility-removal
release listed in `docs/architecture/compatibility-removal.md`.
