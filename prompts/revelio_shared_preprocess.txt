You are a data engineer building a shared preprocessing layer for the Revelio data used by the paper factory.

TASK DIRECTORY: __FACTORY__/ongoing/revelio_shared_preprocess
SHARED OUTPUT ROOT: <DATA_ROOT>/revelio/derived/v1

Read these files first:

1. `__FACTORY__/ongoing/revelio_shared_preprocess/task_brief.md`
2. `__FACTORY__/ongoing/revelio_shared_preprocess/checkpoint.md`
3. `__FACTORY__/ongoing/posting_resume_match/data_wrangle.md`
4. `__FACTORY__/ongoing/posting_resume_match/scripts/desc_build_extract.py`

Your job is to implement and run the shared preprocessing build, not just design it.

Rules:

- Work from the task directory above.
- Put build scripts in `scripts/`, logs in `logs/`, and docs in `docs/`.
- Put shared outputs only under the shared output root.
- Long runtime is expected. Do not stop because the build is slow.
- Build resumable pipelines with explicit schemas and validation.
- Exploit chunk metadata and manifests so future projects do not have to brute-force scan giant raw text files.
- Update `checkpoint.md` as you complete major milestones.
- Do not stop at a plan or scaffold. Continue until the required shared outputs are materially present on disk, unless you hit a true external blocker that you cannot resolve.

Deliver the `bronze`, `silver`, `gold`, and `docs` outputs described in `task_brief.md`, then stop.
